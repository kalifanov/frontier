{
  "name": "frontier-concept",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first tactical autobattler concept",
  "scripts": { "start": "node server.mjs", "build": "node build.mjs", "test": "node --test tests/*.test.js" },
  "engines": { "node": ">=20" }
}
